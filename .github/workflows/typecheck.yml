# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will download a prebuilt Ruby version, install dependencies and run tests with rspec.
# For more information see: https://github.com/marketplace/actions/setup-ruby-jruby-and-truffleruby

name: Typecheck

on:
  push:
    branches: [ master ]
  pull_request:
    branches: ['*']

permissions:
  contents: read

jobs:
  solargraph_typed:
    name: Solargraph / strong

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 4.0
        bundler-cache: false
    - name: Install gems
      run: |
        # Add optional gem to be able to typecheck related code
        echo "gem 'vernier', '>1.0', '<2'" >> .Gemfile
        bundle install
    - name: Compute yardoc cache key
      id: yardoc-cache-key
      run: echo "v=$(bundle list | shasum | cut -c1-16)" >> "$GITHUB_OUTPUT"
    - name: Cache gem yardocs
      uses: actions/cache@v4
      with:
        # Only the yardoc subtree: its path embeds each gem name and version, so a
        # restored entry is either correct or never looked up, and a prefix hit is
        # safe. The sibling .ser pins are keyed on Solargraph::VERSION, which is
        # identical across commits, so carrying those would serve pins built by
        # different lib/ code.
        path: ~/.cache/solargraph/yard-*
        key: sg-yard-${{ runner.os }}-${{ steps.yardoc-cache-key.outputs.v }}
        restore-keys: sg-yard-${{ runner.os }}-
    - name: Install gem types
      run: bundle exec rbs collection install
    - name: Typecheck self
      run: SOLARGRAPH_ASSERTS=on bundle exec solargraph typecheck --level strong
      # @todo Temporary, expect to revert in 0.60
      continue-on-error: true
